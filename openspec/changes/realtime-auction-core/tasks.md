## 1. 工程初始化

- [x] 1.1 创建 Go 后端工程结构，包含 `cmd/api`、`internal/http`、`internal/ws`、`internal/domain/auction`、`internal/service`、`internal/repository`、`internal/redis` 和 `internal/config`
- [x] 1.2 创建 React + TypeScript 前端工程结构，区分商家管理端入口和用户竞拍端入口
- [x] 1.3 添加本地配置示例，覆盖 Go 服务端口、数据库连接、Redis 连接和前端 API 地址
- [x] 1.4 编写本地启动说明，说明 Go 后端、前端、Redis 和 MySQL/PostgreSQL 的运行方式

## 2. 数据模型和持久化

- [x] 2.1 设计并创建商品、竞拍、出价流水和订单表
- [x] 2.2 实现竞拍、出价流水和订单的 repository 接口
- [x] 2.3 实现数据库迁移或初始化脚本，支持本地一键建表
- [x] 2.4 为订单表添加竞拍维度唯一约束，保证同一竞拍最多生成一条有效订单

## 3. 竞拍生命周期

- [x] 3.1 实现竞拍领域模型和 `DRAFT`、`SCHEDULED`、`RUNNING`、`EXTENDED`、`SOLD`、`ENDED`、`CANCELLED` 状态枚举
- [x] 3.2 实现竞拍规则校验，覆盖必填项、起拍价、加价幅度、竞拍时长、封顶价和自动延时配置
- [x] 3.3 实现创建竞拍 API，成功后生成 `DRAFT` 状态竞拍
- [x] 3.4 实现启动竞拍 API，初始化数据库状态和 Redis 热点状态
- [x] 3.5 实现取消竞拍 API，限制只能取消未结束竞拍并广播取消事件
- [x] 3.6 实现竞拍自然结束处理，按是否存在有效出价进入 `SOLD` 或 `ENDED`

## 4. Redis 实时出价核心

- [x] 4.1 定义 Redis key 结构，覆盖竞拍快照、排行榜、幂等请求和房间事件
- [x] 4.2 实现竞拍启动时的 Redis 快照初始化
- [x] 4.3 实现出价原子校验脚本，覆盖状态校验、最低有效出价、加价幅度、封顶价和自动延时
- [x] 4.4 实现 `requestId` 幂等检查，防止重复提交写入多笔出价
- [x] 4.5 实现出价成功后的出价流水落库和 Redis 状态同步
- [x] 4.6 实现封顶价优先于自动延时的成交逻辑

## 5. WebSocket 房间和事件

- [x] 5.1 实现 WebSocket Hub，按 `auctionId` 管理房间连接
- [x] 5.2 实现用户进入房间后的竞拍快照返回
- [x] 5.3 实现出价成功事件广播，包含当前价、最高出价人、排行榜变化、`serverTime` 和 `endsAt`
- [x] 5.4 实现自动延时事件广播，包含新的结束时间和延时原因
- [x] 5.5 实现竞拍结束事件广播，覆盖 `SOLD`、`ENDED` 和 `CANCELLED`
- [x] 5.6 实现断线重连后的快照恢复流程

## 6. 成交和订单

- [x] 6.1 实现封顶价成交后的最小订单生成
- [x] 6.2 实现自然结束成交后的最小订单生成
- [x] 6.3 实现重复结算幂等处理，重复执行时返回已有订单或保持订单不变
- [x] 6.4 实现成交结果查询 API，支持已成交、未成交和已取消竞拍结果

## 7. 前端最小闭环

- [x] 7.1 实现商家创建竞拍页面，支持填写商品信息和竞拍规则
- [x] 7.2 实现商家竞拍管理页面，支持启动、取消和查看结果
- [x] 7.3 实现用户竞拍房间页面，展示商品、规则、当前价、倒计时和排行榜
- [x] 7.4 实现用户出价交互，提交出价并展示成功或失败原因
- [x] 7.5 实现 WebSocket 实时事件处理，刷新当前价、倒计时、排名和结束状态
- [x] 7.6 实现断线重连后的快照刷新提示和页面状态恢复

## 8. 验证和质量

- [x] 8.1 为竞拍规则校验和状态机流转添加 Go 单元测试
- [x] 8.2 为 Redis 出价原子校验添加并发场景测试或集成测试
- [x] 8.3 为订单幂等生成添加测试
- [x] 8.4 编写手工验证清单，覆盖创建竞拍、启动竞拍、用户出价、自动延时、封顶成交、自然结束、取消竞拍和结果查询
- [x] 8.5 运行后端测试和前端构建，确认核心闭环可验证
